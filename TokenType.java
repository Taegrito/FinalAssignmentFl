public enum TokenType {

    // Keywords
    RULE,
    IF,
    THEN,
    AND,

    // Símbolos
    COLON,
    GREATER,
    LESS,
    EQUAL,
    STATE,
    // Datos
    ID,
    VALUE,

    // Fin del archivo
    EOF
}